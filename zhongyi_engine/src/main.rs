#![cfg_attr(debug_assertions, allow(dead_code, unused_imports, unused_variables, unused_mut))]
use zhongyi_engine::unit::{Quantum, Unit, WeightUnit, VolumeUnit};

fn main() {
    println!("Hello, world!");
}
